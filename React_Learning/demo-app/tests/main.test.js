import { it, expect, describe } from 'vitest'
describe('group', () => {   //define the test suite
    it('should', () => {    //test cases
        expect(1).toBeTruthy(); //assertions-expresult,actual result match or not
    })
    
})

